1、projectlevel字段没有。  是 scale
2、需将project中period改为projectperiod，原则是其他表中的代码字段的命名，与其代码表名称一致。对应修改影响到的类文件。不修改数据库。修改相关方法。


3、去掉rddb包，新增加一个com.tdhx.tag包，去掉loginmanager下的pageright.java文件。
4、修改WebRoot\WEB-INF\taglib下的table.tld文件。
5、DbManager下的PoolManager.java中修改dbconfig.properties为dbconfigNole.properties,并在相应的配置路径下更改文件名称
6、userlog中logindate字段，增加默认绑定值：(getdate())，日志就不报错了。
7、修改projectdao中的 getmenulist 和 getmenulist2方法，同时修改projectlist页面中对应的方法。
8、commontools中增加为空的赋值。
9、productdetails中price改为varchar（12）,涉及文件productdetails.java,projectproductview.java,projectproduct.java
10、删除所有的product相关的约束，和productdetails的主键。